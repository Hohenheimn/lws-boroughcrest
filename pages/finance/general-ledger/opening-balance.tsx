import React, { useState } from "react";
import Calendar from "../../../components/Calendar";

export default function OpeningBalance() {
    const [isDate, setDate] = useState("");
    return <div>{/* <Calendar Value={isDate} setValue={setDate} /> */}</div>;
}
